apiVersion: v1
kind: Service
metadata:
  name: {{ include "signal-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: {{ include "signal-api.name" . }}
